import 'package:flutter/material.dart';
import 'package:test_bus/src/theme/theme_data.dart';

class VoyageRouteDate extends StatelessWidget {
  const VoyageRouteDate({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      '14.05.2023, 1 пассажир',
      style: ts12400g6,
    );
  }
}
